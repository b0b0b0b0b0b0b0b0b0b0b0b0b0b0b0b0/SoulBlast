package bm.b0b0b0.SoulBlast.config;

import net.elytrium.serializer.SerializerConfig;

public final class SerializerConfigs {

    public static final SerializerConfig YAML = new SerializerConfig.Builder().build();

    private SerializerConfigs() {
    }

}
